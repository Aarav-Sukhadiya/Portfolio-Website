# CLAUDE.md

@AGENTS.md

## Claude Instructions

- Treat `AGENTS.md` as the canonical project guidance and follow it for every task in this repository.
- Inspect the relevant existing components, data, styles, and documentation before editing.
- Preserve the current architecture unless the requested change requires replacing it.
- Keep the live site and preview mode synchronized when changing shared visual behavior.
- Run `npm run build` after code or configuration changes; do not claim nonexistent lint, typecheck, or test validation.
- Perform relevant desktop, mobile, keyboard, touch, and reduced-motion checks for UI changes.
- Never commit or push without the user's explicit permission for that specific action.
